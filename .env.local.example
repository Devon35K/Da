# Copy this file to .env.local (gitignored) and fill in your key.
# This lets the frontend call OpenAI directly when the Django backend is not running.
# Only use for local development — never expose this key in production.

VITE_OPENAI_API_KEY=sk-...
VITE_OPENAI_MODEL=gpt-4o-mini
